require 'spec_helper'

describe Sociographer do
  it 'has a version number' do
    expect(Sociographer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
